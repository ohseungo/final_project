<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="product">
	<insert id="insertProduct" parameterType="Product">
	insert into PRODUCT (PRODUCTID, PRODUCTVALUE, PRODUCTNAME, PRODUCTCONTENT, COMPID) 
	VALUES (#{productId}, #{productValue}, #{productName}, #{productContent}, #{compId})
	</insert>
	<select id="selectProduct" parameterType="String" resultType="Product">
		select PRODUCTID, PRODUCTVALUE, PRODUCTNAME, PRODUCTCONTENT, COMPID
		from PRODUCT
		where PRODUCTID = #{id}
	</select>
	<select id="selectProductList" resultType="Product">
		select PRODUCTID,PRODUCTVALUE,PRODUCTNAME,PRODUCTCONTENT,COMPID 
		from PRODUCT
	</select>
	<delete id="deleteProduct" parameterType="String">
		delete from PRODUCT
		where PRODUCTID = #{id}
	</delete>
</mapper>